# Copy to .env and fill in your real key. Never commit .env.
GROQ_API_KEY=gsk_your-key-here
MODEL=llama-3.3-70b-versatile
# Groq's free tier is ~free, so cost defaults to 0. Set real prices if you go paid.
INPUT_PRICE=0.0
OUTPUT_PRICE=0.0
